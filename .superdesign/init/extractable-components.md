# Extractable Components

No source app exists yet. Components should be extracted from the Remotion implementation only when repeated visual behavior appears at least twice.

Candidate extraction threshold:

- Keep one-off cinematic shots inline.
- Extract `SlabCard`, `SourceChip`, `Subtitle`, `ChapterTitle`, `ScenarioBars`, and `MetricTile` because they repeat across scenes.
