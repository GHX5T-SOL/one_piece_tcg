# Video Archive

The prior Remotion/video workspace remains in `src/`, `hyperframes/`, and `out/`.

No final MP4 was present in `dist/` during this workspace restructuring pass. If the earlier `ghost-zoro-one-piece-tcg-investment.mp4` is restored or re-rendered, place it here and add it to the media manifest.
